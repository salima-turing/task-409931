// notification.js

class Notification {
    static sendAlert(message) {
        console.log("Sending alert:", message);
        // Implement actual notification logic (e.g., email, SMS, webhook)
    }
}

module.exports = Notification;
